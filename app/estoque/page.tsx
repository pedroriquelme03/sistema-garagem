"use client";
import Link from "next/link";
import { useEffect, useMemo, useState } from "react";
const exemplo = [["Chevrolet", "Onix Plus LT", "2022/2023", "RHK-2E19", "42.300 km", "R$ 82.900", "Anunciado"], ["Toyota", "Corolla XEi", "2021/2022", "SDP-8A04", "61.700 km", "R$ 122.900", "Preparação"], ["Volkswagen", "T-Cross", "2023/2024", "RKL-1G22", "18.200 km", "R$ 119.900", "Anunciado"]];
export default function Estoque() { const [search, setSearch] = useState(""); const [added, setAdded] = useState<string[][]>([]); useEffect(() => { try { const saved: Record<string, string>[] = JSON.parse(localStorage.getItem("garagem-pro-estoque") ?? "[]"); setAdded(saved.map(v => [v.marca || "Sem marca", v.modelo || "Sem modelo", [v.anoFabricacao, v.anoModelo].filter(Boolean).join("/") || "—", v.placa || "—", v.km ? `${v.km} km` : "—", v.valorVenda || "—", "Cadastrado"])); } catch {} }, []); const list = useMemo(() => [...added, ...exemplo].filter(v => v.join(" ").toLowerCase().includes(search.toLowerCase())), [added, search]); return <div className="mx-auto max-w-7xl px-4 py-7 sm:px-6 lg:px-8"><header className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-brand-600">Operação</p><h1 className="mt-1 text-3xl font-bold text-slate-950">Estoque</h1><p className="mt-1 text-sm text-slate-500">{28 + added.length} veículos cadastrados.</p></div><Link href="/adicionar-veiculo" className="rounded-xl bg-brand-600 px-4 py-2.5 text-center text-sm font-semibold text-white">+ Adicionar veículo</Link></header><div className="mt-7 overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-100 p-4"><input value={search} onChange={e => setSearch(e.target.value)} placeholder="Buscar marca, modelo ou placa" className="w-full rounded-xl border border-slate-200 px-4 py-2.5 text-sm outline-none focus:border-brand-500 sm:w-80" /></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left"><thead className="bg-slate-50 text-xs uppercase text-slate-400"><tr><th className="px-6 py-3">Veículo</th><th>Ano</th><th>Km</th><th>Preço</th><th className="px-6">Status</th></tr></thead><tbody className="divide-y divide-slate-100">{list.map(v => <tr key={v[3]}><td className="px-6 py-4"><p className="font-semibold text-slate-800">{v[0]} {v[1]}</p><p className="text-xs text-slate-500">{v[3]}</p></td><td>{v[2]}</td><td>{v[4]}</td><td className="font-semibold">{v[5]}</td><td className="px-6"><span className="rounded-full bg-brand-50 px-2.5 py-1 text-xs font-semibold text-brand-700">{v[6]}</span></td></tr>)}</tbody></table></div></div></div>; }
