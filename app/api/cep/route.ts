import { NextRequest, NextResponse } from "next/server";
export const dynamic = "force-dynamic";
export async function GET(req: NextRequest) { const cep = (req.nextUrl.searchParams.get("cep") ?? "").replace(/\D/g, ""); if (!/^\d{8}$/.test(cep)) return NextResponse.json({ ok: false, error: "Informe um CEP com 8 dígitos." }, { status: 400 }); try { const r = await fetch(`https://viacep.com.br/ws/${cep}/json/`, { cache: "no-store" }); const d = await r.json(); if (!r.ok || d.erro) return NextResponse.json({ ok: false, error: "CEP não encontrado." }, { status: 404 }); return NextResponse.json({ ok: true, data: { cep: d.cep, endereco: d.logradouro, bairro: d.bairro, municipio: d.localidade, uf: d.uf, codigoMunicipio: d.ibge, complementoCep: d.complemento } }); } catch { return NextResponse.json({ ok: false, error: "Não foi possível consultar o CEP agora." }, { status: 502 }); } }
